{
  "name": "com.ateo.build",
  "version": "0.6.3",
  "displayName": "Ateo Build",
  "description": "In-repo build definitions and a unified headless build entry point for the Ateo Unity build server. Builds are authored as committed ScriptableObjects; CI and local builds run through the same code path. Distributed as a UPM git package, pinned per project.",
  "unity": "6000.0",
  "keywords": [
    "build",
    "ci",
    "automation"
  ],
  "author": {
    "name": "Ateo"
  }
}
